import 'package:flutter/material.dart';

class MyPackageExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Contoh Penggunaan Package')),
      body: Center(
        child: Text(
          'Hello with Google Fonts!',
        ),
      ),
    );
  }
}
